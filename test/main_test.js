var expect = require('chai').expect;

// test suite

describe('Mocha', () => {
	// test spec (unit test)
	it('should run our test using npm', () => {
		expect(true).to.be.ok; //ok is an assertion value that is truthy
	});
});